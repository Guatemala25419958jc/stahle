"use client";

import { useEffect, useMemo, useState } from "react";
import { BarChart3, ClipboardList, LogOut, Package, Plus, Search, Settings2, Users, Trash2, Star } from "lucide-react";
import { products as publicProducts } from "../data";
import { AdminSummary } from "./AdminSummary";

const ADMIN_EMAIL = "stahleegt@gmail.com";
const statuses = ["Pendiente", "En proceso", "Cotizada", "Cerrada"] as const;

type Quote = { id:number; customer:string; project:string; status:typeof statuses[number]; date:string };
const initialQuotes: Quote[] = [
  {id:1, customer:"Aún no hay solicitudes", project:"Las nuevas cotizaciones aparecerán aquí", status:"Pendiente", date:"—"}
];

export default function AdminPage() {
  const [password, setPassword] = useState("");
  const [authError, setAuthError] = useState(false);
  const [authorized, setAuthorized] = useState(false);
  const [section, setSection] = useState("Resumen");
  const [query, setQuery] = useState("");
  const [quotes, setQuotes] = useState(initialQuotes);
  const [products, setProducts] = useState<Array<{id:string;name:string;collection:string;room:string;images?:string[]}>>([]);
  const [productName, setProductName] = useState("");
  const [productDescription, setProductDescription] = useState("");
  const [productMaterials, setProductMaterials] = useState("");
  const [productDimensions, setProductDimensions] = useState("");
  const [productPrice, setProductPrice] = useState("");
  const [productCollection, setProductCollection] = useState("obsidian");
  const [productRoom, setProductRoom] = useState("sala");
  const [productImages, setProductImages] = useState("");
  const [imageFiles, setImageFiles] = useState<File[]>([]);
  const [coverIndex, setCoverIndex] = useState(0);
  const [saveError, setSaveError] = useState("");
  const [showProductForm, setShowProductForm] = useState(false);
  const [customerName, setCustomerName] = useState("");
  const [customerPhone, setCustomerPhone] = useState("");
  const [customerEmail, setCustomerEmail] = useState("");
  const [customers, setCustomers] = useState<Array<{id:string;name:string;phone:string;email:string}>>([]);
  const [quoteCustomer, setQuoteCustomer] = useState("");
  const [quoteProject, setQuoteProject] = useState("");
  useEffect(() => { fetch("/api/admin/products").then(r => r.ok ? r.json() : []).then((items) => setProducts(items.length ? items : publicProducts.map((p) => ({id:p.slug,name:p.name,collection:p.collection,room:"sala",images:p.images})))).catch(() => setProducts([])); fetch("/api/admin/customers").then(r => r.ok ? r.json() : []).then(setCustomers).catch(() => setCustomers([])); }, []);
  const addCustomer = async (event: React.FormEvent) => { event.preventDefault(); const response = await fetch("/api/admin/customers", {method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({name:customerName, phone:customerPhone, email:customerEmail})}); if(response.ok){ const item=await response.json(); setCustomers([...customers,{id:item.id,name:customerName,phone:customerPhone,email:customerEmail}]); setCustomerName(""); setCustomerPhone(""); setCustomerEmail(""); } };
  const addQuote = async (event: React.FormEvent) => { event.preventDefault(); const response=await fetch("/api/admin/quotes",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({customer_name:quoteCustomer,product_type:quoteProject})}); if(response.ok){setQuotes([{id:Date.now(),customer:quoteCustomer,project:quoteProject,status:"Pendiente",date:new Date().toLocaleDateString("es-GT")},...quotes]);setQuoteCustomer("");setQuoteProject("");} };
  const addProduct = async (event: React.FormEvent) => { event.preventDefault(); setSaveError(""); const uploaded:string[]=[]; for (const file of imageFiles) { const form=new FormData(); form.append("file",file); const upload=await fetch("/api/admin/images",{method:"POST",body:form}); if(upload.ok){ const item=await upload.json(); uploaded.push(item.url); } } const response = await fetch("/api/admin/products", {method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({name:productName, collection:productCollection, room:productRoom, description:productDescription, materials:productMaterials.split(",").map(x=>x.trim()).filter(Boolean), dimensions:productDimensions, price:productPrice || "Precio por confirmar", images:(() => { const all=[...uploaded,...productImages.split(",").map(x=>x.trim()).filter(Boolean)]; if(all.length && coverIndex>0){ const cover=all.splice(coverIndex,1)[0]; all.unshift(cover); } return all; })()})}); if(response.ok){ const item=await response.json(); setProducts([...products,{id:item.id,name:productName,collection:productCollection,room:productRoom,images:uploaded}]); setProductName(""); setProductDescription(""); setProductMaterials(""); setProductDimensions(""); setProductPrice(""); setImageFiles([]); setCoverIndex(0); } else { const error=await response.json().catch(()=>({})); setSaveError(error.error ?? "No se pudo guardar. Verifica que el build más reciente esté activo."); } };

  const filteredQuotes = useMemo(() => quotes.filter((q) =>
    [q.customer, q.project, q.status].join(" ").toLowerCase().includes(query.toLowerCase())
  ), [quotes, query]);

  if (!authorized) {
    return <main className="admin-auth" style={{backgroundImage:`linear-gradient(90deg,rgba(7,6,5,.9),rgba(7,6,5,.55)),url("${publicProducts[0]?.images?.[0] ?? "/hero-stahle.png"}")`}}><div className="admin-auth-card"><img className="admin-logo-auth" src="/stahle-isologo-transparent.png" alt="Stahlé"/><p className="admin-eyebrow">STAHLÉ · PANEL PRIVADO</p><h1>Administración</h1><p>Acceso exclusivo para el equipo de Stahlé.</p><form onSubmit={async (event) => { event.preventDefault(); setAuthError(false); const response = await fetch("/api/admin/auth", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({password}) }); if (response.ok) setAuthorized(true); else setAuthError(true); }}><label>Contraseña de administrador<input type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Contraseña privada" required /></label>{authError && <small className="admin-error">Contraseña incorrecta.</small>}<button className="admin-primary" type="submit">Entrar al panel</button></form></div></main>;
  }

  const nav = [
    {label:"Resumen", icon:BarChart3},
    {label:"Administrar galería", icon:Package}
  ];

  return <main className="admin-shell"><aside className="admin-sidebar"><div className="admin-brand"><img src="/stahle-isologo-transparent.png" alt=""/><div>STAHLÉ<small>ADMIN</small></div></div><nav>{nav.map(({label,icon:Icon}) => <button key={label} className={section===label ? "active" : ""} onClick={() => setSection(label)}><Icon size={17}/>{label}</button>)}</nav><button className="admin-logout" onClick={() => setAuthorized(false)}><LogOut size={16}/>Cerrar sesión</button></aside><section className="admin-main"><header className="admin-header"><div><p className="admin-eyebrow">PANEL PRIVADO</p><h1>{section}</h1></div><div className="admin-user"><span>Administrador</span><b>SE</b></div></header>{section==="Resumen" && <AdminSummary products={products} onGallery={()=>setSection("Administrar galería")} onAdd={()=>{setSection("Administrar galería");setShowProductForm(true)}}/>}{(section==="Productos" || section==="Administrar galería") && <div className="admin-panel"><div className="admin-panel-head"><div><p className="admin-eyebrow">CATÁLOGO Y FOTOGRAFÍAS</p><h3>Administrar galería</h3><p className="admin-note">Gestiona los productos de tu catálogo. Agrega, edita o elimina piezas.</p></div><button className="admin-primary" type="button" onClick={()=>setShowProductForm(true)}><Plus size={16}/>Agregar producto</button></div><div className="admin-gallery-toolbar"><label className="admin-search"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar en la galería"/></label><select><option>Todas las colecciones</option><option>Obsidian</option><option>Savia</option><option>Áurea</option><option>Vértice</option></select><select><option>Todos los ambientes</option><option>Sala</option><option>Comedor</option><option>Dormitorio</option></select></div>{showProductForm && <form className="admin-product-form" onSubmit={addProduct}><input value={productName} onChange={e=>setProductName(e.target.value)} placeholder="Nombre del producto" required/><textarea value={productDescription} onChange={e=>setProductDescription(e.target.value)} placeholder="Descripción de la pieza" rows={3}/><input value={productMaterials} onChange={e=>setProductMaterials(e.target.value)} placeholder="Materiales (separados por coma)"/><input value={productDimensions} onChange={e=>setProductDimensions(e.target.value)} placeholder="Medidas (ej. 120 × 70 × 40 cm)"/><input value={productPrice} onChange={e=>setProductPrice(e.target.value)} placeholder="Precio"/><select value={productCollection} onChange={e=>setProductCollection(e.target.value)}><option value="obsidian">Obsidian</option><option value="savia">Savia</option><option value="aurea">Áurea</option><option value="vertice">Vértice</option></select><select value={productRoom} onChange={e=>setProductRoom(e.target.value)}><option value="sala">Sala</option><option value="comedor">Comedor</option><option value="dormitorio">Dormitorio</option><option value="cocina">Cocina</option><option value="bano">Baño</option><option value="oficina">Oficina</option></select><label className="admin-upload"><strong>Subir fotografías</strong><input type="file" accept="image/*" multiple onChange={e=>{setImageFiles(Array.from(e.target.files ?? []));setCoverIndex(0)}}/><small>{imageFiles.length ? `${imageFiles.length} foto(s) seleccionada(s)` : "Haz clic aquí para elegir imágenes desde tu computadora"}</small></label>{imageFiles.length > 0 && <label className="admin-cover">Portada del producto<select value={coverIndex} onChange={e=>setCoverIndex(Number(e.target.value))}>{imageFiles.map((file,index)=><option key={`${file.name}-${index}`} value={index}>Foto {index+1}: {file.name}</option>)}</select></label>}{saveError && <small className="admin-error">{saveError}</small>}<button className="admin-primary" type="submit"><Plus size={16}/>Guardar producto</button></form>}{products.length===0 ? <div className="admin-empty"><Package size={28}/><p>Aún no hay productos administrables.</p></div> : <div className="admin-table">{products.map(p=><div className="admin-gallery-card" key={p.id}><div className="admin-gallery-thumb">{p.images?.[0] ? <img src={p.images[0]} alt={p.name}/> : <Package size={24}/>}<span><Star size={12}/> Portada</span></div><div className="admin-gallery-info"><strong>{p.name}</strong><small>{p.collection} · {p.room}</small><div><button className="admin-link" type="button">Editar</button><button className="admin-delete" type="button" onClick={async()=>{if(confirm("¿Eliminar este producto?")){const r=await fetch(`/api/admin/products?id=${p.id}`,{method:"DELETE"});if(r.ok)setProducts(products.filter(x=>x.id!==p.id));}}}><Trash2 size={14}/>Eliminar</button></div></div></div>)}</div>}</div>}{section==="Clientes" && <div className="admin-panel"><div className="admin-panel-head"><div><p className="admin-eyebrow">RELACIONES</p><h3>Clientes</h3></div></div><form className="admin-product-form" onSubmit={addCustomer}><input value={customerName} onChange={e=>setCustomerName(e.target.value)} placeholder="Nombre completo" required/><input value={customerPhone} onChange={e=>setCustomerPhone(e.target.value)} placeholder="Teléfono"/><input type="email" value={customerEmail} onChange={e=>setCustomerEmail(e.target.value)} placeholder="Correo"/><button className="admin-primary" type="submit"><Plus size={16}/>Guardar cliente</button></form>{customers.length===0 ? <div className="admin-empty"><Users size={28}/><p>Aún no hay clientes registrados.</p></div> : <div className="admin-table">{customers.map(c=><div className="admin-table-row" key={c.id}><span>{c.name}</span><span>{c.phone}</span><span>{c.email}</span><span>Registrado</span></div>)}</div>}</div>}{section==="Cotizaciones" && <div className="admin-panel"><div className="admin-panel-head"><div><p className="admin-eyebrow">VENTAS</p><h3>Cotizaciones</h3></div><label className="admin-search"><Search size={16}/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Buscar solicitud"/></label></div><form className="admin-product-form" onSubmit={addQuote}><input value={quoteCustomer} onChange={e=>setQuoteCustomer(e.target.value)} placeholder="Cliente" required/><input value={quoteProject} onChange={e=>setQuoteProject(e.target.value)} placeholder="Proyecto o mueble" required/><button className="admin-primary" type="submit"><Plus size={16}/>Nueva cotización</button></form><div className="admin-table"><div className="admin-table-row admin-table-head"><span>Cliente</span><span>Proyecto</span><span>Estado</span><span>Fecha</span></div>{filteredQuotes.map(q=><div className="admin-table-row" key={q.id}><span>{q.customer}</span><span>{q.project}</span><span><i className="status-dot"/>{q.status}</span><span>{q.date}</span></div>)}</div></div>}{section==="Configuración" && <div className="admin-panel"><p className="admin-eyebrow">SEGURIDAD</p><h3>Acceso administrativo</h3><p className="admin-note">El acceso está reservado para <strong>{ADMIN_EMAIL}</strong>. La protección definitiva del enlace se completará con Cloudflare Access antes de publicar este panel.</p></div>}</section></main>;
}
